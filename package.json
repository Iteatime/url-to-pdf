{
  "name": "url-to-pdf",
  "version": "0.0.0",
  "description": "A PDF generator using Puppeteer",
  "main": "dist/index.js",
  "private": true,
  "scripts": {
    "build": "tsc",
    "dev": "NODE_ENV=development nodemon --exec 'ts-node' ./src/",
    "test": "echo \"Error: no test specified\" && exit 1",
    "test-unit": "mocha -r ts-node/register src/**/*.spec.ts  --watch --watch-extensions ts"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/express": "^4.16.1",
    "@types/node-cleanup": "^2.1.0",
    "@types/puppeteer": "^1.12.1",
    "express": "^4.16.4",
    "node-cleanup": "^2.1.2",
    "puppeteer": "^1.12.1",
    "ts-node": "^8.0.2",
    "typescript": "^3.3.1"
  },
  "devDependencies": {}
}
